import express from 'express';
//import cors from 'cors';
import { Request, Response, NextFunction } from 'express';

const app = express();

// 미들웨어 및 라우터 설정
// app.use(...);

// app 객체를 내보내기

export {app};